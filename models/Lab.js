// const mongoose = require('mongoose');

// Maybe in the future, overkill for now
// const labSchema = new mongoose.Schema({
//   name: { type: String, required: true }, // Only name for now, though in the future we could add more variables
// });

// module.exports = mongoose.model('Lab', labSchema);
// Unused. Opted to just use a simple function used in homepage.js
exports.search = (req, res) => {
//   const query = req.query.q || '';
  
//   const results = allLabs.filter(lab =>
//     lab.name.toLowerCase().includes(query.toLowerCase()) ||
//     lab.id.toLowerCase().includes(query.toLowerCase())
//   );
  
//   res.json({
//     success: true,
//     results: results
//   });
};
